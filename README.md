# AlgoSelector

AlgoSelector is a Streamlit-based application for **tabular data profiling, preprocessing, and algorithm recommendation**.  
It helps users explore datasets, handle missing values, encode categorical features, scale numeric features, and get ML algorithm suggestions.

## 🚀 Features
- Upload CSV datasets
- Automatic profiling (shape, stats, categorical summaries)
- Missing value handling (drop, mean, mode)
- Encoding + scaling
- Class imbalance detection
- Algorithm recommendation engine

## 🛠️ Installation
Clone the repository and install dependencies:

```bash
git clone https://github.com/your-username/AlgoSelector.git
cd AlgoSelector
pip install -r requirements.txt
```

## ▶️ Usage
Run the Streamlit app:

```bash
streamlit run ml-logic/Home.py
```